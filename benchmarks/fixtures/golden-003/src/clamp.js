// biome-ignore lint/correctness/noUnusedFunctionParameters: This Golden fixture intentionally starts with an upper-bound bug.
export const clamp = (value, minimum, maximum) => Math.max(minimum, value);
